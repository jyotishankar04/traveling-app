// Re-export shared skeletons so existing admin imports keep working while
// the implementation lives in one place.
export { TableSkeleton, StatsSkeleton } from "@/components/custom/shared/StateViews"